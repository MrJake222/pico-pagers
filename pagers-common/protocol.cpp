#include <protocol.hpp>


int hash(const uint8_t *data, size_t data_len, uint8_t *hash, size_t *hash_len) {
    // TODO implement
    return 0;
}

int crypto_sign(uint8_t *private_key, uint8_t *hash, size_t hash_len) {
    // TODO implement
    return 0;
}

int crypto_verify(uint8_t *public_key, uint8_t *hash, size_t hash_len) {
    // TODO implement
    return 0;
}
